.venv/
.venv-serve/
__pycache__/
*.pyc
.pytest_cache/
.ipynb_checkpoints/

# Local Claude Code files — keep local, never commit.
CLAUDE.md
.claude/

# Raw downloads and large artifacts are reproducible, not committed.
data/raw/
data/processed/
models/*.pkl
models/*.pt
models/*.joblib
models/weights/
# ...except the served model, committed so deploys are fast and offline.
!models/baseline.joblib
