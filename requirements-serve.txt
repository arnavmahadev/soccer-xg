# Runtime-only dependencies for the deployed container — kept minimal.
# Serving loads a raw XGBoost Booster (models/baseline.ubj) via the lightweight
# `predictor` module, so scikit-learn / joblib / torch / pandas are NOT needed.
fastapi
uvicorn[standard]
pydantic
numpy
xgboost-cpu        # same `xgboost` module, without the ~400MB CUDA/GPU libs
