# Core schema + tests
pydantic
pytest

# Data acquisition & EDA
statsbombpy
pandas
pyarrow
matplotlib

# Features & models
numpy
scikit-learn
xgboost            # needs OpenMP at runtime (macOS: brew install libomp)
torch              # MLP comparison (Phase 4)

# API
fastapi
uvicorn[standard]
httpx              # FastAPI TestClient
